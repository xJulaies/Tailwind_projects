export class Contract {
  constructor(id, name, car, service, date, status) {
    this.id = id;
    this.name = name;
    this.car = car;
    this.service = service;
    this.date = date;
    this.status = status;
  }
}
